import { Doc } from '@root/layout/Doc';
import { CustomScrollbarExample } from '@root/examples/slider/custom-scrollbar';

{/* DOC_START */}

# Custom Scrollbar Example

This is a slider which displays a custom scrollbar using the [`<SliderProgress />`](/docs/slider/api#slider-progress) component. This component can be placed anywhere in your DOM. The native scrollbar is hidden from the track basic css. Scroll or drag the slider to see the effect.

<CustomScrollbarExample />

{/* DOC_END */}

export default (props) => <Doc
  {...props}
  githubUrl="/slider/examples/custom-scrollbar.mdx"
  metaTitle="Example Custom Scrollbar Slider"
  metaDescription="Example custom scrollbar slider using the @faceless-ui/slider npm module."
  metaURL="/docs/slider/examples/custom-scrollbar"
/>

