import { Doc } from '@root/layout/Doc';
import { MarqueeSliderExample } from '@root/examples/slider/marquee';

{/* DOC_START */}

# Marquee Slider Example

This is a marquee slider that slowly scrolls through the slides, as opposed to sliding one-by-slide. For more details, see [marquee](/docs/slider/api#marquee) prop.

<MarqueeSliderExample />

{/* DOC_END */}

export default (props) => <Doc
  {...props}
  githubUrl={`/slider/examples/marquee.mdx`}
  metaTitle="Example Marquee Slider"
  metaDescription="Example marquee slider using the @faceless-ui/slider npm module."
  metaURL="/docs/slider/examples/marquee"
/>

