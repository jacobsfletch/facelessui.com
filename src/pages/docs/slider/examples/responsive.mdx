import { Doc } from '@root/layout/Doc';
import { ResponsiveSliderExample } from '@root/examples/slider/responsive';

# Responsive Slider Example

This is a responsive slider that shows two slides at a time on desktop, then only 1 slide at screens smaller than 576px wide. You can override any slider setting using any CSS media query using the [breakpoints](/docs/slider/api#breakpoints) prop.

<ResponsiveSliderExample />

export default (props) => <Doc
  {...props}
  githubUrl={`/slider/examples/responsive.mdx`}
  metaTitle="Example Responsive Slider"
  metaDescription="Example responsive slider using the @faceless-ui/slider npm module."
  metaURL="/docs/slider/examples/responsive"
/>

