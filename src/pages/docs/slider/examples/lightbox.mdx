import { Doc } from '@root/layout/Doc';
import { LightboxSliderExample } from '@root/examples/slider/lightbox';

# Lightbox Scrollbar Example

This is a "lightbox slider" that opens the slide in a larger view when clicked. Click on any slide to see the effect. See [synchronized sliders](/docs/slider/setup#sync) for more information.

<LightboxSliderExample />

export default (props) => <Doc
  {...props}
  githubUrl={`/slider/examples/lightbox.mdx`}
  metaTitle="Example Lightbox Slider"
  metaDescription="Example lightbox slider using the @faceless-ui/slider npm module."
  metaURL="/docs/slider/examples/lightbox"
/>

