import { Doc } from '@root/layout/Doc';
import { SnapSliderExample } from '@root/examples/slider/snap';

# Snap Slider Example

This slider snaps to each slide as the user navigates them. This uses [CSS Scroll Snap](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Scroll_Snap) behind the scenes. Scroll to see the effect.

<SnapSliderExample />

export default (props) => <Doc
  {...props}
  githubUrl={`/slider/examples/snap.mdx`}
  metaTitle="Example Snap Slider"
  metaDescription="Example snap slider using the @faceless-ui/slider npm module."
  metaURL="/docs/slider/examples/snap"
/>

