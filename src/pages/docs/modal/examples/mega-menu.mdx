import { Doc } from '@root/layout/Doc';
import { MegaMenuExample } from '@root/examples/modal/MegaMenu';

{/* DOC_START */}

# Mega Menu Example

This is a "mega menu" that opens a modal with a list of menu items that, when clicked, open their own modal with additional content.

<MegaMenuExample />

{/* DOC_END */}

export default (props) => <Doc
  {...props}
  githubUrl="/modal/examples/mega-menu.mdx"
  metaTitle="Mega Menu Example"
  metaDescription="Mega menu example using the @faceless-ui/modal npm module."
  metaURL="/docs/modal/examples/mega-menu"
/>
