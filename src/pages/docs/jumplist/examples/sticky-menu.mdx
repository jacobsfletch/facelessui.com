import { Doc } from '@root/layout/Doc';
import { StickyMenuExample } from '@root/examples/jumplist/StickyMenu';

{/* DOC_START */}

# Sticky Menu Example

This is an example menu which sticks to the top of the viewport and highlights the current section as you scroll the page. Scroll down or open the menu and click a nav item to see the effect.

<StickyMenuExample />

{/* DOC_END */}

export default (props) => <Doc
  {...props}
  githubUrl="/jumplist/examples/sticky-menu.mdx"
  metaTitle="Example Sticky Menu"
  metaDescription="Example sticky menu using the @faceless-ui/jumplist npm module."
  metaURL="/docs/jumplist/examples/sticky-menu"
/>
